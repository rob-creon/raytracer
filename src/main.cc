#include "img_writer.hh"
#include "vec3.hh"

const unsigned camera_img_width = 300;
const unsigned camera_img_height = 300;

void render() {
  for (int j = 0; j < camera_img_height; ++j) {
    for (int i = 0; i < camera_img_width; ++i) {
    }
  }
}

int main() {
  cr3on_rt_vec::test_vec3();
  render();
}
